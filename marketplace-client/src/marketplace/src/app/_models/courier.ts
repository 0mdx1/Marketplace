import {Role} from './role';

export class Courier {
  name?: string;
  surname?: string;
  password?: string;
  email?: string;
  dateOfBirth?: string;
  phone?: string;
  role?: Role;
  active?: boolean;
}
